@model UsuarioModel
@*@model Int32*@

@{
    ViewData["Title"] = "Confirmarção apagar de usuário";
}

<!--<div class="text-center">
    <h3>Realmente deseja apagar o usuário-->@*(@Model.)*@<!--?</h3>
</div>-->
<div class="text-center">
    <h3>Realmente deseja apagar o usuário(@Model.Nome)?</h3>
</div>

<a class="btn btn-danger btn-block" asp-controller="Usuario" asp-action="ApagarPost" asp-route-id="@Model.Id" role="button">Confirmar exclusão</a>
<a class="btn btn-secondary btn-block" asp-controller="Usuario" asp-action="IndexUsuario" role="button">Voltar</a>
